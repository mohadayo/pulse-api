# PulseAPI Environment Configuration

# Service ports (host-side mapping)
COLLECTOR_PORT=8080
AGGREGATOR_PORT=8081
NOTIFIER_PORT=8082

# Logging level
# Python: DEBUG, INFO, WARNING, ERROR
# Go/TypeScript: debug, info, warn, error
LOG_LEVEL=INFO
